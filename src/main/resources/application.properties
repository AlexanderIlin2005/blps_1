# Server configuration
server.port=8080

# Database configuration - ТОЧНО КАК В ВАШЕМ XML
spring.datasource.url=jdbc:postgresql://localhost:5432/studs
spring.datasource.username=s381032
spring.datasource.password=UVos.8847
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate configuration - МИНИМАЛЬНО, как в EclipseLink
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# Убираем ВСЕ упоминания схемы - вы и так в ней
# Убираем currentSchema из URL
# Убираем hibernate.default_schema
# Убираем все дополнительные параметры

# Allow circular references (оставляем только это)
spring.main.allow-circular-references=true

# Logging
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
