{
  "name": "AntiFraud Check",
  "description": "Blocks order if user has made too many orders in the last hour",
  "priority": 1,
  "condition": "redisTemplate.opsForValue().get('antifraud:user:' + user.getId() + ':orderCount') != null && (int) redisTemplate.opsForValue().get('antifraud:user:' + user.getId() + ':orderCount') >= 5",
  "action": {
    "handler": "blockActionHandler",
    "params": {
      "reason": "Too many orders in the last hour"
    }
  }
}
